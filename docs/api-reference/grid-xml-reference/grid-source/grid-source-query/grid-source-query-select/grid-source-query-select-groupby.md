# grid > source > query > select > groupBy

The `<groupBy>` element is used to configure the grouping of a query result.

It has no attributes and can have zero or more `<column>` child attributes.

### Example:

```html
<groupBy>
    <column name="status"/>
</groupBy>
```

